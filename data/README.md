# Local Data Directory

This directory is for local-only electoral roll PDFs, parsed voter exports, and generated validation outputs.

Do not commit raw PDFs, parsed voter records, full EPIC values, addresses, or other bulk voter data. The repository `.gitignore` keeps this directory ignored except for this note.
